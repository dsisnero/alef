name: {{ shard_name }}
version: {{ version }}

description: |
  Crystal bindings for {{ crate_name }}, generated by alef.

crystal: ">= 1.0.0"

license: MIT

targets:
  {{ shard_name }}:
    main: src/{{ shard_name }}.cr
